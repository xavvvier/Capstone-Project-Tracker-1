	<div class="ui two column stackable centered grid">
		<div class="column">
			<h1>Edit Campus
				<a class="ui animated right floated secondary basic button" href="@Url.Action("Index", "Campus")">
					<div class="visible content">Back</div>
					<div class="hidden content">
						<i class="left arrow icon"></i>
					</div>
				</a>
			</h1>
			<form class="ui form" method="post" action="@Url.Action("Edit", "Campus")">
                <div class="field">
                    <label>Name</label>
                    <input type="text" required name="Name" maxlength="100" placeholder="Campus">
                </div>
                <button class="ui animated positive basic button" type="submit">
					<div class="visible content">Save Changes</div>
					<div class="hidden content">
						<i class="save icon"></i>
					</div>
				</button>
			</form>
		</div>
	</div>

